diff -ruN a/osu-wine b/osu-wine
--- a/osu-wine	2026-07-02 23:14:58.000000000 -0230
+++ b/osu-wine	2026-08-07 01:07:19.813189510 -0230
@@ -21,13 +21,18 @@
 
 set -a
 # Don't change this 'winello-default' file! It'll be overwritten on updates.
-source "$XDG_DATA_HOME/osuconfig/update/stuff/winello-default.cfg"
+source "${WINELLO_ROOT:-$XDG_DATA_HOME/osuconfig/update}/stuff/winello-default.cfg"
 set +a
 
-export WINE="${WINE:-$XDG_DATA_HOME/osuconfig/yawl-winello}"
-export WINESERVER="${WINESERVER:-${WINE}server}"
 export WINEPREFIX="${WINEPREFIX:-$XDG_DATA_HOME/wineprefixes/osu-wineprefix}"
-export WINE_INSTALL_PATH="${WINE_INSTALL_PATH:-$XDG_DATA_HOME/osuconfig/wine-osu}"
+if [ "${WINELLO_PACKAGED:-0}" = "1" ]; then
+    export WINE="${WINE:-$XDG_DATA_HOME/osuconfig/yawl-winello}"
+    export WINE_INSTALL_PATH="${WINE_INSTALL_PATH:-/opt/wine-osu}"
+else
+    export WINE="${WINE:-$XDG_DATA_HOME/osuconfig/yawl-winello}"
+    export WINE_INSTALL_PATH="${WINE_INSTALL_PATH:-$XDG_DATA_HOME/osuconfig/wine-osu}"
+fi
+export WINESERVER="${WINESERVER:-${WINE}server}"
 
 export WINETRICKS="${WINETRICKS:-$XDG_DATA_HOME/osuconfig/winetricks}"
 
@@ -84,6 +89,12 @@
 
 # Won't return failure if there's no internet connection, but tries to update the main script first before calling the function
 MainScriptUpdate() {
+    if [ "${WINELLO_PACKAGED:-0}" = "1" ]; then
+        mkdir -p "$XDG_DATA_HOME/osuconfig"
+        ln -sfn "${WINELLO_ROOT:-/usr/share/osu-stable}" "$XDG_DATA_HOME/osuconfig/update"
+        MainScript "${@:-}"
+        return $?
+    fi
     _clone() { GIT_TERMINAL_PROMPT=0 git clone "https://github.com/NelloKudo/osu-winello.git" "$XDG_DATA_HOME/osuconfig/update" --quiet 2>/dev/null; }
     _pull() { GIT_TERMINAL_PROMPT=0 git -C "$XDG_DATA_HOME/osuconfig/update" pull --quiet 2>/dev/null; }
     _reset() { GIT_TERMINAL_PROMPT=0 git -C "$XDG_DATA_HOME/osuconfig/update" reset --hard "${1:-HEAD}" --quiet 2>/dev/null; }
@@ -108,7 +119,9 @@
     [[ "${POST_ARGS[0]}" = exec* ]] && CMD=("exec" "bash") && POST_ARGS=("${POST_ARGS[@]:1}")
     [ -z "${POST_ARGS[*]}" ] && ErrorAndExit "(internal) Missing arguments?"
 
-    "${CMD[@]}" "$XDG_DATA_HOME/osuconfig/update/osu-winello.sh" "${POST_ARGS[@]}"
+    local _script="$XDG_DATA_HOME/osuconfig/update/osu-winello.sh"
+    [ "${WINELLO_PACKAGED:-0}" = "1" ] && _script="${WINELLO_ROOT:-/usr/share/osu-stable}/osu-winello.sh"
+    "${CMD[@]}" "$_script" "${POST_ARGS[@]}"
     return $? # shouldn't reach this if using exec
 }
 
diff -ruN a/osu-winello.sh b/osu-winello.sh
--- a/osu-winello.sh	2026-07-02 23:14:58.000000000 -0230
+++ b/osu-winello.sh	2026-08-07 01:07:19.813391060 -0230
@@ -47,6 +47,17 @@
 # The full path to osu-winello.sh
 SCRPATH="$(realpath "$0")"
 
+if [ "${WINELLO_PACKAGED:-0}" = "1" ]; then
+    WINELLO_ROOT="${WINELLO_ROOT:-/usr/share/osu-stable}"
+    export WINE_INSTALL_PATH="${WINE_INSTALL_PATH:-/opt/wine-osu}"
+    _xdgh="${XDG_DATA_HOME:-$HOME/.local/share}"
+    export YAWL_INSTALL_PATH="${YAWL_INSTALL_PATH:-$_xdgh/osuconfig/yawl}"
+    export WINE="${WINE:-$_xdgh/osuconfig/yawl-winello}"
+    export WINESERVER="${WINESERVER:-${WINE}server}"
+    PACKAGED_PREFIX="${PACKAGED_PREFIX:-/usr/share/osu-stable/osu-winello-prefix.tar.xz}"
+    [ -d "$WINELLO_ROOT/stuff" ] && SCRDIR="$WINELLO_ROOT"
+fi
+
 # Exported global variables
 
 export XDG_DATA_HOME="${XDG_DATA_HOME:-$HOME/.local/share}"
@@ -209,11 +220,15 @@
 # Function looking for basic stuff needed for installation
 InitialSetup() {
     # Better to not run the script as root, right?
-    if [ "$USER" = "root" ]; then InstallError "Please run the script without root"; fi
+    if [ "$(id -u)" -eq 0 ] || [ "$USER" = "root" ]; then
+        InstallError "Please run as your normal user - never as root/sudo"
+    fi
 
     # Checking for previous versions of osu-wine (mine or DiamondBurned's)
-    if [ -e /usr/bin/osu-wine ]; then Quit "Please uninstall old osu-wine (/usr/bin/osu-wine) before installing!"; fi
-    if [ -e "$BINDIR/osu-wine" ]; then Quit "Please uninstall Winello (osu-wine --remove) before installing!"; fi
+    if [ "${WINELLO_PACKAGED:-0}" != "1" ]; then
+        if [ -e /usr/bin/osu-wine ]; then Quit "Please uninstall old osu-wine (/usr/bin/osu-wine) before installing!"; fi
+        if [ -e "$BINDIR/osu-wine" ]; then Quit "Please uninstall Winello (osu-wine --remove) before installing!"; fi
+    fi
 
     Info "Welcome to the script! Follow it to install osu! 8)"
 
@@ -272,6 +287,35 @@
 
 # Function to install script files, yawl and Wine-osu
 InstallWine() {
+    mkdir -p "$XDG_DATA_HOME/osuconfig" "$BINDIR"
+
+    if [ "${WINELLO_PACKAGED:-0}" = "1" ]; then
+        Info "Using packaged osu-stable..."
+        ln -sfn /usr/bin/osu-stable "$BINDIR/osu-stable"
+        # Drop stale launcher from older winello/package names
+        if [ -L "$BINDIR/osu-wine" ] || [ -e "$BINDIR/osu-wine" ]; then
+            rm -f "$BINDIR/osu-wine"
+        fi
+        mkdir -p "$XDG_DATA_HOME/icons" "$XDG_DATA_HOME/osuconfig"
+        cp "${SCRDIR}/stuff/osu-wine.png" "$XDG_DATA_HOME/icons/osu-stable.png" && chmod 644 "$XDG_DATA_HOME/icons/osu-stable.png"
+        rm -rf "$XDG_DATA_HOME/osuconfig/wine-osu"
+        ln -sfn /opt/wine-osu "$XDG_DATA_HOME/osuconfig/wine-osu"
+        LASTWINEVERSION="$WINEVERSION"
+        echo "$LASTWINEVERSION" >"$XDG_DATA_HOME/osuconfig/wineverupdate"
+
+        install -Dm755 /usr/lib/osu-stable/yawl "$XDG_DATA_HOME/osuconfig/yawl"
+        # Handler must live under $HOME so yawl enter / pressure-vessel can see it
+        install -Dm755 "${SCRDIR}/stuff/osu-handler-wine" "$XDG_DATA_HOME/osuconfig/osu-handler-wine"
+        export YAWL_INSTALL_PATH="$XDG_DATA_HOME/osuconfig/yawl"
+        export WINE="$XDG_DATA_HOME/osuconfig/yawl-winello"
+        export WINESERVER="${WINE}server"
+        YAWL_VERBS="make_wrapper=winello;exec=$WINE_INSTALL_PATH/bin/wine;wineserver=$WINE_INSTALL_PATH/bin/wineserver"             "$YAWL_INSTALL_PATH" || InstallError "Couldn't create yawl wrapper."
+        YAWL_VERBS="update;verify;exec=/bin/true" "$YAWL_INSTALL_PATH" || InstallError "yawl Steam Runtime setup failed."
+
+        ln -sfn "${WINELLO_ROOT:-/usr/share/osu-stable}" "$XDG_DATA_HOME/osuconfig/update"
+        return 0
+    fi
+
     # Installing game launcher and related...
     Info "Installing game script:"
     cp "${SCRDIR}/osu-wine" "$BINDIR/osu-wine" && chmod +x "$BINDIR/osu-wine"
@@ -375,7 +419,11 @@
         # Downloading prefix in temporary ~/.winellotmp folder
         # to make up for this issue: https://github.com/NelloKudo/osu-winello/issues/36
         mkdir -p "$HOME/.winellotmp"
-        DownloadFile "${PREFIXLINK}" "$HOME/.winellotmp/osu-winello-prefix.tar.xz" || Revert
+        if [ "${WINELLO_PACKAGED:-0}" = "1" ] && [ -r "${PACKAGED_PREFIX:-/usr/share/osu-stable/osu-winello-prefix.tar.xz}" ]; then
+            cp "${PACKAGED_PREFIX:-/usr/share/osu-stable/osu-winello-prefix.tar.xz}" "$HOME/.winellotmp/osu-winello-prefix.tar.xz" || Revert
+        else
+            DownloadFile "${PREFIXLINK}" "$HOME/.winellotmp/osu-winello-prefix.tar.xz" || Revert
+        fi
 
         # Checking whether to create prefix manually or install it from repos
         if [ "$failprefix" = "true" ]; then
@@ -537,7 +585,11 @@
 
     Info "Downloading prefix.. this might take a while."
     mkdir -p "$HOME/.winellotmp"
-    DownloadFile "${PREFIXLINK}" "$HOME/.winellotmp/osu-winello-prefix.tar.xz" || { rm -rf "$HOME/.winellotmp" && return 1; }
+    if [ "${WINELLO_PACKAGED:-0}" = "1" ] && [ -r "${PACKAGED_PREFIX:-/usr/share/osu-stable/osu-winello-prefix.tar.xz}" ]; then
+        cp "${PACKAGED_PREFIX:-/usr/share/osu-stable/osu-winello-prefix.tar.xz}" "$HOME/.winellotmp/osu-winello-prefix.tar.xz" || { rm -rf "$HOME/.winellotmp" && return 1; }
+    else
+        DownloadFile "${PREFIXLINK}" "$HOME/.winellotmp/osu-winello-prefix.tar.xz" || { rm -rf "$HOME/.winellotmp" && return 1; }
+    fi
 
     Info "Extracting prefix.."
     tar -xf "$HOME/.winellotmp/osu-winello-prefix.tar.xz" -C "$XDG_DATA_HOME/wineprefixes" || { rm -rf "$HOME/.winellotmp" && return 1; }
@@ -654,18 +706,24 @@
     [ -r "$XDG_DATA_HOME/osuconfig/wineverupdate" ] && LASTWINEVERSION=$(</"$XDG_DATA_HOME/osuconfig/wineverupdate")
 
     if [ "$LASTWINEVERSION" \!= "$WINEVERSION" ]; then
-        # Downloading Wine..
-        DownloadFile "$WINELINK" "/tmp/wine-osu-winello-fonts-wow64-$MAJOR.$MINOR-$PATCH-x86_64.tar.xz" || return 1
-
-        # This will extract Wine-osu and set last version to the one downloaded
-        Info "Updating Wine-osu"...
-        rm -rf "$XDG_DATA_HOME/osuconfig/wine-osu"
-        tar -xf "/tmp/wine-osu-winello-fonts-wow64-$MAJOR.$MINOR-$PATCH-x86_64.tar.xz" -C "$XDG_DATA_HOME/osuconfig"
-        rm -f "/tmp/wine-osu-winello-fonts-wow64-$MAJOR.$MINOR-$PATCH-x86_64.tar.xz"
+        if [ "${WINELLO_PACKAGED:-0}" = "1" ]; then
+            Info "Wine-osu comes from pacman (wine-osu-stable)."
+            ln -sfn /opt/wine-osu "$XDG_DATA_HOME/osuconfig/wine-osu"
+            echo "$WINEVERSION" >"$XDG_DATA_HOME/osuconfig/wineverupdate"
+        else
+            # Downloading Wine..
+            DownloadFile "$WINELINK" "/tmp/wine-osu-winello-fonts-wow64-$MAJOR.$MINOR-$PATCH-x86_64.tar.xz" || return 1
 
-        echo "$WINEVERSION" >"$XDG_DATA_HOME/osuconfig/wineverupdate"
-        Info "Update is completed!"
-        waitWine wineboot -u
+            # This will extract Wine-osu and set last version to the one downloaded
+            Info "Updating Wine-osu"...
+            rm -rf "$XDG_DATA_HOME/osuconfig/wine-osu"
+            tar -xf "/tmp/wine-osu-winello-fonts-wow64-$MAJOR.$MINOR-$PATCH-x86_64.tar.xz" -C "$XDG_DATA_HOME/osuconfig"
+            rm -f "/tmp/wine-osu-winello-fonts-wow64-$MAJOR.$MINOR-$PATCH-x86_64.tar.xz"
+
+            echo "$WINEVERSION" >"$XDG_DATA_HOME/osuconfig/wineverupdate"
+            Info "Update is completed!"
+            waitWine wineboot -u
+        fi
     else
         Info "Your Wine-osu is already up-to-date!"
     fi
@@ -939,6 +997,13 @@
 osuHandlerSetup() {
     Info "Configuring osu-mime and osu-handler..."
 
+    if [ "${WINELLO_PACKAGED:-0}" = "1" ]; then
+        install -Dm755 "${SCRDIR}/stuff/osu-handler-wine" "$XDG_DATA_HOME/osuconfig/osu-handler-wine"
+        waitWine regedit /s "${SCRDIR}/stuff/osu-handler.reg"
+        $okay
+        return 0
+    fi
+
     # Installing osu-mime from https://aur.archlinux.org/packages/osu-mime
     DownloadFile "${OSUMIMELINK}" "/tmp/osu-mime.tar.gz" || return 1
 
@@ -985,40 +1050,63 @@
 
 # Open files/links with osu-handler-wine
 osuHandlerHandle() {
-    local ARG="${*:-}" OSUPID
-    local -a PRE_ARGS
-    local HANDLERRUN=("$XDG_DATA_HOME/osuconfig/update/stuff/osu-handler-wine")
-    [ ! -x "${HANDLERRUN[0]}" ] && chmod +x "${HANDLERRUN[0]}"
+    local ARG="${*:-}" OSUPID HANDLERBIN
+    local -a PRE_ARGS HANDLERRUN
 
-    if [ -x "$YAWL_INSTALL_PATH" ] && OSUPID="$(pgrep osu!.exe)"; then
-        HANDLERRUN=("env" "YAWL_VERBS=enter=$OSUPID" "$YAWL_INSTALL_PATH" "${HANDLERRUN[0]}")
+    # Prefer a copy under $HOME (visible to yawl enter / pressure-vessel), matching nix-osu-stable
+    HANDLERBIN="$XDG_DATA_HOME/osuconfig/osu-handler-wine"
+    if [ ! -x "$HANDLERBIN" ]; then
+        local src="${WINELLO_ROOT:-$XDG_DATA_HOME/osuconfig/update}/stuff/osu-handler-wine"
+        [ -x "$src" ] || src="$XDG_DATA_HOME/osuconfig/update/stuff/osu-handler-wine"
+        install -Dm755 "$src" "$HANDLERBIN" || {
+            Error "Missing osu-handler-wine"
+            return 1
+        }
+    fi
+
+    if [ -x "$YAWL_INSTALL_PATH" ] && OSUPID="$(pgrep -n 'osu!.exe' 2>/dev/null || true)" && [ -n "$OSUPID" ]; then
+        # Call yawl directly with enter=PID (do not nest steam-run / userns)
+        HANDLERRUN=("env" "YAWL_INSTALL_DIR=$(dirname "$YAWL_INSTALL_PATH")" "YAWL_VERBS=enter=$OSUPID" "$YAWL_INSTALL_PATH" "$HANDLERBIN")
         echo "Trying to open osu-handler-wine in the running container for osu! (PID=$OSUPID)" >&2
     else
         IFS=" " read -r -a PRE_ARGS <<<"env ${PRE_LAUNCH_ARGS}"
-        HANDLERRUN=("${PRE_ARGS[@]}" "${WINE}") # we don't actually need osu-handler if we're starting a new instance
+        HANDLERRUN=("${PRE_ARGS[@]}" "${WINE}")
         echo "Trying to open a new instance of osu! to handle ${ARG}" >&2
     fi
 
     case "$ARG" in
     osu://*)
         echo "Trying to load link ($ARG).." >&2
-        exec "${HANDLERRUN[@]}" 'C:\\windows\\system32\\start.exe' "$ARG"
+        cd "$HOME"
+        exec "${HANDLERRUN[@]}" 'C:\windows\system32\start.exe' "$ARG"
         ;;
     *.osr | *.osz | *.osk | *.osz2)
-        local EXT="${ARG##*.}" FULLARGPATH FILEDIR
-        FULLARGPATH="$(realpath "${ARG}")" || FULLARGPATH="${ARG}" # || for fallback if realpath failed
-
-        # also, add the containing directory to the PRESSURE_VESSEL_FILESYSTEMS_RW, because it might be in some other location
-        FILEDIR="$(realpath "$(dirname "${FULLARGPATH}")")"
-        if [ -n "${FILEDIR}" ] && [ "${FILEDIR}" != "/" ]; then
-            export PRESSURE_VESSEL_FILESYSTEMS_RW="${PRESSURE_VESSEL_FILESYSTEMS_RW}:${FILEDIR}"
+        local EXT="${ARG##*.}" FULLARGPATH OSUPATH base staged winefile
+        FULLARGPATH="$(realpath "${ARG}" 2>/dev/null || true)"
+        FULLARGPATH="${FULLARGPATH:-$ARG}"
+
+        # Stage onto the game drive (D:) so osu!'s MoveFile is same-volume.
+        # Opening from Z:\...\Downloads often yields "Error moving file" under Wine.
+        if [ -r "$XDG_DATA_HOME/osuconfig/osupath" ]; then
+            OSUPATH="$(<"$XDG_DATA_HOME/osuconfig/osupath")"
+        else
+            OSUPATH="${OSUPATH:-$XDG_DATA_HOME/osu-wine/osu!}"
         fi
-
-        echo "Trying to load file ($FULLARGPATH).." >&2
-        exec "${HANDLERRUN[@]}" 'C:\\windows\\system32\\start.exe' "/ProgIDOpen" "osustable.File.$EXT" "$FULLARGPATH"
+        [ -f "$FULLARGPATH" ] || {
+            Error "File not found: $FULLARGPATH"
+            return 1
+        }
+        mkdir -p "$OSUPATH/.handler-import"
+        base="$(basename "$FULLARGPATH")"
+        staged="$OSUPATH/.handler-import/$base"
+        cp -f "$FULLARGPATH" "$staged"
+        winefile="D:\\.handler-import\\$base"
+
+        echo "Trying to load file ($FULLARGPATH -> $winefile).." >&2
+        cd "$HOME"
+        exec "${HANDLERRUN[@]}" 'C:\windows\system32\start.exe' "/ProgIDOpen" "osustable.File.$EXT" "$winefile"
         ;;
     esac
-    # If we reached here, it must means osu-handler failed/none of the cases matched
     Error "Unsupported osu! file ($ARG) !" >&2
     Error "Try running \"bash $SCRPATH fixosuhandler\" !" >&2
     return 1
